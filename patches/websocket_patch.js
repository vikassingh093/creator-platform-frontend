/**
 * ============================================================
 * 🔌 WEBSOCKET PATCH — Frontend
 * ============================================================
 * HOW TO APPLY:
 *   1. npm install socket.io-client
 *   2. Copy useCallSocket.js to src/hooks/
 *   3. Add VITE_USE_WEBSOCKET=true to .env
 *   4. Update HomePage.jsx (replace polling useEffect)
 *   5. Update CreatorDashboard.jsx (replace polling useEffect)
 *
 * HOW TO REVERT:
 *   1. Change VITE_USE_WEBSOCKET=false in .env
 *   2. That's it! Code auto-falls back to polling.
 * ============================================================
 */